# multi-doc-answer-pipeline
An n8n-powered intelligent query resolution pipeline that first checks structured Excel data for known answers, then escalates unmatched queries to multiple LLMs. Responses are evaluated by an LLM-based judge to select the most accurate answer. Designed for scalable, multi-source document intelligence and dynamic response generation.
